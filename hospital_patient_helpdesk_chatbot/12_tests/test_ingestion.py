"""Test document loading, cleaning, chunking, and metadata."""

import pytest


@pytest.mark.skip(reason="Enable after the corresponding module is implemented.")
def test_placeholder() -> None:
    assert True
